/** Default warning threshold applied to newly created budgets. */
export const DEFAULT_WARNING_THRESHOLD_PERCENT = 80;

/** Fallback currency for the whole app (Malaysian Ringgit). */
export const DEFAULT_CURRENCY = "MYR";

export const CATEGORY_ICON_FALLBACK = "MoreHorizontal";
